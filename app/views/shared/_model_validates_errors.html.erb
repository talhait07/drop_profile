<% if obj.present? && obj.errors.present? %>
  <h3>Following <%= pluralize(obj.errors.count, 'error') %> occurs:</h3>
  <ul>
    <% obj.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
